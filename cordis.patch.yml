# @lihua/dsh-mcp-manager —— 宿主插件实例（CLI 安装时随 dsh.bundle.patch 自动应用）
# 注意：本文件不写任何路径——cordisPath/patchPath 由插件在运行时按 profile 动态探测（配置/环境变量可覆盖）
- insert:
    - id: mcp-manager
      name: '@lihua/dsh-mcp-manager'
